import { Theme, createTheme } from "@mui/material/styles";

export const colorTheme: Theme = createTheme({
  palette: {
    primary: {
      main: "#e8eaf6",
    },
    secondary: {
      main: "#aee0ae",
    },
  },
});
